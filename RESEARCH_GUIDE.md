# 研究记录指南

这份指南用于让不同子项目仍然可以被理解、复现和横向比较。它规定的是最低信息量，不限制子项目采用自己的工程结构。

## 从问题开始

一个合格的研究问题应当说明：研究对象、希望验证的能力、观察指标，以及什么证据会推翻当前假设。

推荐写法：

> 在给定约束与测试集下，方案 A 是否比基线 B 更稳定地完成任务 C？

避免只写“研究某某库”。范围过大时，先拆成多个可独立回答的问题。

## 每个项目至少记录

- **研究问题**：要回答的具体问题。
- **状态与日期**：当前阶段、创建时间和最近更新时间。
- **假设**：实验前的判断以及判断依据。
- **方法**：环境、输入、步骤、指标和对照组。
- **复现方式**：从干净环境开始所需的命令与依赖版本。
- **证据**：日志、截图、数据、测试结果或可访问的演示。
- **结论与局限**：证据支持什么、不支持什么，以及后续问题。

## 推荐目录

```text
projects/<project-id>/
├─ README.md       # 研究入口、结论与复现步骤
├─ project.json    # 供仓库索引读取的元数据
├─ src/            # 实现代码（按需创建）
├─ tests/          # 可重复的验证（按需创建）
├─ notes/          # 阶段记录（按需创建）
├─ data/           # 小型、可公开的数据（按需创建）
└─ demo/           # 可部署演示（按需创建）
```

大型数据、模型权重、密钥和私人材料不要直接提交。应在 README 中记录取得方式、校验值与权限要求，并只提交可公开的最小样例。

## 状态定义

| 状态 | 含义 | 进入条件 |
| --- | --- | --- |
| `planned` | 计划中 | 问题和范围已定义 |
| `active` | 进行中 | 已有正在执行的实验或实现 |
| `paused` | 暂停 | 保留上下文，但短期不继续 |
| `completed` | 已完成 | 结论、证据与复现步骤齐全 |
| `archived` | 已归档 | 不再维护，仅保留历史价值 |

## 结果与可复现性

- 尽量固定运行时和关键依赖版本。
- 保存输入条件、随机种子、硬件或外部服务版本。
- 区分原始观察、解释和推断。
- 失败实验同样有价值；记录失败条件，不只保留成功结果。
- 对会变化的外部服务标注测试日期。
- 展示页面服务于快速浏览，子项目 README 才是完整研究记录。

## 完成检查

项目转为 `completed` 前，至少确认：

- 新读者能从 README 理解问题、方法和结论。
- 在文档声明的环境中可以执行关键复现步骤。
- 结论可以追溯到仓库中的证据或稳定外链。
- 已说明已知限制、成本、安全和许可边界。
- `project.json`、`projects/catalog.json` 与根 README 索引一致。
