# 《左耳听风》研究胶囊：从慢 SQL 事故到系统性故障学习

> 文档性质：研究者自写的结构化摘要与合成案例，不包含《左耳听风》专栏原文摘录。
> 使用目的：验证 LLM Wiki 能否把一个安全、可公开的研究输入编译为可追溯、可编辑、可关联的知识页面。

## 1. 来源与边界

此前研究使用一个第三方公开专栏镜像作为受控代理语料。固定快照包含 119 个 Markdown 文件和 162 张图片；它不是纸质书全文，也不能外推为对纸质书逐章完成了分析。镜像没有发现可供本研究依赖的再分发许可，因此本实验不复制、不上传其中的正文。

本胶囊只保留研究者自己的结构化理解、统计口径与一个明确标注的合成事故。任何生成结论都应把本文件列入 `sources`，并把“研究者派生材料”与“原始专栏事实”区分开。

## 2. 合成事故：慢 SQL 引发的服务雪崩

某业务发布后，数据库出现一条高频慢 SQL。查询占满连接池，接口延迟升高，重试进一步放大数据库压力，最终导致部分服务不可用。团队最初把根因写成“开发人员 SQL 写得慢”，并把整改写成“优化 SQL、加强责任心”。

进一步复盘得到以下合成观察：

1. 查询计划与索引变化在发布前没有被检查，性能回归没有进入发布门禁。
2. 告警主要依赖邮件，值班人员在故障发生约 27 分钟后才确认异常。
3. 监控覆盖 CPU 与错误率，但缺少连接池占用、慢查询分布、关键代理层指标和重试放大系数。
4. 发布流程没有明确要求压测、容量评估或 DBA 评审，也没有可验证的豁免记录。
5. 应急预案没有规定限流、熔断、降级和回滚的触发条件，现场处置依赖个人经验。
6. 如果只处罚写 SQL 的个人，其他系统条件仍然存在，下一次事故可能由另一条查询或另一个服务再次触发。

这些内容是为了检验分析方法而编写的合成事实，不对应真实公司或真实事故。

## 3. 关键区分：触发因素不等于充分根因

慢 SQL 是直接触发因素，但“它发生了”并不能充分解释“为什么一个局部性能问题可以升级为线上故障”。系统性根因分析至少要继续检查四层条件：

- 技术机制：索引、查询计划、连接池、重试、限流、熔断和资源隔离。
- 工程控制：性能测试、容量基线、发布门禁、回滚路径和可观测性。
- 协作流程：评审责任、值班响应、升级路径、跨团队信息同步。
- 组织学习：复盘是否寻找可改变的系统条件，是否把归责误当成治理。

这并不意味着个人行为永远无关，而是要求整改优先回答：哪些机制允许单点错误穿透多层防线？哪些控制措施能够被验证为真的改变了风险？

## 4. 方法：系统性故障学习审查

一次可复用的故障学习审查可以按以下顺序执行：

1. 建立时间线，区分已证实事实、推断和未知项。
2. 描述直接触发与影响扩散链，不把第一处错误直接命名为根因。
3. 从技术机制、工程控制、协作流程和组织条件四层继续追问。
4. 为每项整改说明它改变的风险机制，例如减少发生概率、缩短发现时间、限制爆炸半径或加快恢复。
5. 每项整改必须包含负责人、期限、验收证据、复查日期和复发验证方式。
6. 保留仍未解决的证据缺口，进入 Review，而不是用确定语气填补未知。

“优化 SQL”可以是必要动作，但不是完整闭环。完整闭环还应覆盖发布前发现、运行时发现、影响隔离、恢复能力和复盘后的验证。

## 5. 期望形成的知识结构

本实验希望模型识别并建立以下可编辑知识，但不强制使用这些文件名：

- 一个合成慢 SQL 事故案例页。
- 一个“直接触发与系统根因”概念页。
- 一个“系统性故障学习审查”方法页。
- 一个“不以归责替代治理”的边界或原则页。
- 页面之间的 `[[wikilink]]`，以及回到本胶囊的 `sources` 追踪。

生成页面是模型对本胶囊的派生理解，不是新的事实来源。关键事实必须回到本胶囊核验，未知项应进入 Review 或明确标注。
